import { ConvertCurencyPipe } from './convert-curency.pipe';

describe('ConvertCurencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertCurencyPipe();
    expect(pipe).toBeTruthy();
  });
});
